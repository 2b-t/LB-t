os: linux
dist: bionic

language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-9
      - g++-9

script:
  - export CC=gcc-9
  - export CXX=g++-9
  - make
